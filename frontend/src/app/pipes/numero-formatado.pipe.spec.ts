import { NumeroFormatadoPipe } from './numero-formatado.pipe';

describe('NumeroFormatadoPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroFormatadoPipe();
    expect(pipe).toBeTruthy();
  });
});
